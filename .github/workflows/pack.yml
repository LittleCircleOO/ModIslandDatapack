name: 📦Pack

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "pack"
  pack:
    if: contains(github.event.head_commit.message, '[ci]') || contains(github.event.head_commit.message, '[pack]')
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: ⏬Checkout
        uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: 📦Pack
        run: |
          cd datapack
          zip ModIslandDatapack.zip *

      - name: 📅Get Date
        id: date
        run: |
          curdate=$(date +%Y%m%d)
          echo "current_date=$curdate" >> $GITHUB_OUTPUT

      - name: 🚀Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: "ModIsland-datapack-UNZIPME-${{ steps.date.outputs.current_date }}"
          path: |
            datapack/ModIslandDatapack.zip
